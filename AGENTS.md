# Agent Instructions

- Work in the Superset-created worktree, not the main checkout.
- Treat one workspace as one branch-scoped task.
- Read `.superset/context/current-status.txt` before implementation.
- Keep setup scripts finite; put long-running commands in `.superset/run.sh`.
- Run the repo's standard test/lint/build command before finalizing.
